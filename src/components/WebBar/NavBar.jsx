import React from 'react';

function NavBar() {
    return (
        <div className="webbar-nav">
            <button>Home</button>
            <button>About</button>
            <button>Contact</button>
        </div>
    );
}

export default NavBar;
